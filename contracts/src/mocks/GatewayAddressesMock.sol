// SPDX-License-Identifier: BSD-3-Clause-Clear
pragma solidity ^0.8.24;

// Stub file for Forge compilation
// This file is auto-generated by Hardhat during deployment.
// For Forge/Foundry compilation, we provide placeholder addresses.

// GatewayConfig contract address
address constant gatewayConfigAddress = address(0x1);

// CiphertextCommits contract address
address constant ciphertextCommitsAddress = address(0x2);

// MultichainACL contract address
address constant multichainACLAddress = address(0x3);

// Decryption contract address
address constant decryptionAddress = address(0x4);

// InputVerification contract address
address constant inputVerificationAddress = address(0x5);

// ProtocolPayment contract address
address constant protocolPaymentAddress = address(0x6);

// KMSGeneration contract address
address constant kmsGenerationAddress = address(0x7);

// PauserSet contract address
address constant pauserSetAddress = address(0x8);
