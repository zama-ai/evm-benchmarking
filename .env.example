# Blockchain RPC endpoint
ETH_RPC_URL=http://127.0.0.1:8545

# Root account credentials (choose one):
# - PRIVATE_KEY: Single private key (takes precedence over MNEMONIC for root account)
# - MNEMONIC: BIP-39 phrase for reproducible root account derivation
#             MNEMONIC is required for test that create and fund multiple accounts so that funds are recoverable.

#   Also used for deterministic test account generation when passed to fundTestAccounts()
MNEMONIC="test test test test test test test test test test test junk"

# xk6-output-influxdb uses v2 write API compatibility endpoint
K6_INFLUXDB_ADDR=http://localhost:8086
K6_INFLUXDB_BUCKET=gateway-benchmarks        # Maps to database name in InfluxDB v3
K6_INFLUXDB_TOKEN="my-auth-token"

# Optional InfluxDB settings:
K6_INFLUXDB_PUSH_INTERVAL=1s
K6_INFLUXDB_CONCURRENT_WRITES=4
